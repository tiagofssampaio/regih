# Migration `20201122203253-init`

This migration has been generated by Tiago Sampaio at 11/22/2020, 8:32:53 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `Client` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `name` VARCHAR(191) NOT NULL,
    `vatId` VARCHAR(191) NOT NULL,
    `rate` DECIMAL(65,30) NOT NULL,
    `createdAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `Task` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `clientId` INT NOT NULL,
    `description` VARCHAR(191) NOT NULL,
    `specialRate` DECIMAL(65,30) NOT NULL,
    `createdAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `TaskTime` (
    `taskId` INT NOT NULL,
    `startTime` DATETIME(3) NOT NULL,
    `endTime` DATETIME(3) NOT NULL,
UNIQUE INDEX `TaskTime.taskId_startTime_endTime_unique`(`taskId`,
`startTime`,
`endTime`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `Invoice` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `value` DECIMAL(65,30) NOT NULL,
    `file` VARCHAR(191) NOT NULL,
    `is_payed` BOOLEAN NOT NULL,
    `pay_date` DATETIME(3) NOT NULL,
    `comment` VARCHAR(191) NOT NULL,
    `createdAt` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `TaskInvoice` (
    `taskId` INT NOT NULL,
    `invoiceId` INT NOT NULL,
UNIQUE INDEX `TaskInvoice.taskId_invoiceId_unique`(`taskId`,
`invoiceId`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `Task` ADD FOREIGN KEY (`clientId`) REFERENCES `Client`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `TaskTime` ADD FOREIGN KEY (`taskId`) REFERENCES `Task`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `TaskInvoice` ADD FOREIGN KEY (`taskId`) REFERENCES `Task`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `TaskInvoice` ADD FOREIGN KEY (`invoiceId`) REFERENCES `Invoice`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201122203253-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,55 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Client {
+  id            Int      @default(autoincrement()) @id
+  name          String
+  vatId         String
+  rate          Float
+  createdAt     DateTime @default(now())
+}
+
+model Task {
+  id            Int      @default(autoincrement()) @id
+  clientId      Int
+  description   String
+  specialRate   Float
+  createdAt     DateTime @default(now())
+  client        Client   @relation(fields: [clientId], references: [id])
+//  times         TaskTime[]
+}
+
+model TaskTime {
+    taskId      Int
+    startTime   DateTime
+    endTime     DateTime
+    task        Task     @relation(fields: [taskId], references: [id])
+    @@unique([taskId, startTime, endTime])
+}
+
+model Invoice {
+    id          Int      @default(autoincrement()) @id
+    value       Float
+    file        String
+    is_payed    Boolean
+    pay_date    DateTime
+    comment     String
+    createdAt   DateTime @default(now())
+}
+
+model TaskInvoice {
+    taskId      Int
+    invoiceId   Int
+    task        Task    @relation(fields: [taskId], references: [id])
+    invoice     Invoice @relation(fields: [invoiceId], references: [id])
+    @@unique([taskId, invoiceId])
+}
```


