# Migration `20201122232639-init`

This migration has been generated by Tiago Sampaio at 11/22/2020, 11:26:39 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `client` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `name` VARCHAR(191) NOT NULL,
    `vat_id` VARCHAR(191) NOT NULL,
    `rate` DECIMAL(65,30) NOT NULL,
    `created_at` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `task` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `client_id` INT NOT NULL,
    `description` VARCHAR(191) NOT NULL,
    `special_rate` DECIMAL(65,30) NOT NULL,
    `created_at` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
INDEX `client_id`(`client_id`),

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `task_time` (
    `task_id` INT NOT NULL,
    `start_time` DATETIME(3) NOT NULL,
    `end_time` DATETIME(3) NOT NULL,
UNIQUE INDEX `TASK_TIME_TASK_ID_START_TIME_END_TIME`(`task_id`,
`start_time`,
`end_time`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `invoice` (
    `id` INT NOT NULL AUTO_INCREMENT,
    `value` DECIMAL(65,30) NOT NULL,
    `file` VARCHAR(191) NOT NULL,
    `is_payed` BOOLEAN NOT NULL,
    `pay_date` DATETIME(3) NOT NULL,
    `comment` VARCHAR(191) NOT NULL,
    `created_at` DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),

    PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `task_invoice` (
    `task_id` INT NOT NULL,
    `invoice_id` INT NOT NULL,
UNIQUE INDEX `TASK_INVOICE_TASK_ID_INVOICE_ID`(`task_id`,
`invoice_id`),
INDEX `task_id`(`task_id`),
INDEX `invoice_id`(`invoice_id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `task` ADD FOREIGN KEY (`client_id`) REFERENCES `client`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `task_time` ADD FOREIGN KEY (`task_id`) REFERENCES `task`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `task_invoice` ADD FOREIGN KEY (`task_id`) REFERENCES `task`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `task_invoice` ADD FOREIGN KEY (`invoice_id`) REFERENCES `invoice`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201122232639-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,68 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "mysql"
+  url = "***"
+}
+
+model Client {
+  id        Int      @id @default(autoincrement())
+  name      String
+  vatId     String   @map("vat_id")
+  rate      Float
+  createdAt DateTime @default(now()) @map("created_at")
+  task      Task[]
+
+  @@map("client")
+}
+
+model Task {
+  id           Int           @id @default(autoincrement())
+  clientId     Int           @map("client_id")
+  description  String
+  specialRate  Float         @map("special_rate")
+  createdAt    DateTime      @default(now()) @map("created_at")
+  client       Client        @relation(fields: [clientId], references: [id])
+  invoices     TaskInvoice[]
+  times        TaskTime[]
+
+  @@index([clientId], name: "client_id")
+  @@map("task")
+}
+
+model TaskTime {
+  taskId     Int    @map("task_id")
+  startTime DateTime   @map("start_time")
+  endTime   DateTime   @map("end_time")
+  task       Task     @relation(fields: [taskId], references: [id])
+
+  @@unique([taskId, startTime, endTime], name: "TASK_TIME_TASK_ID_START_TIME_END_TIME")
+  @@map("task_time")
+}
+
+model Invoice {
+  id        Int           @id @default(autoincrement())
+  value     Float
+  file      String
+  isPayed   Boolean       @map("is_payed")
+  payDate   DateTime      @map("pay_date")
+  comment   String
+  createdAt DateTime      @default(now()) @map("created_at")
+  tasks     TaskInvoice[]
+
+  @@map("invoice")
+}
+
+model TaskInvoice {
+  taskId    Int @map("task_id")
+  invoiceId Int @map("invoice_id")
+task       Task    @relation(fields: [taskId], references: [id])
+  invoice    Invoice @relation(fields: [invoiceId], references: [id])
+
+  @@unique([taskId, invoiceId], name: "TASK_INVOICE_TASK_ID_INVOICE_ID")
+    @@index([taskId], name: "task_id")
+  @@index([invoiceId], name: "invoice_id")
+  @@map("task_invoice")
+}
```


